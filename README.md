# Gmail to D1

A Cloudflare template that syncs your Gmail into a D1 database.

Deploy it on your own Cloudflare account. Connect Gmail with OAuth. Query your mail from a small web UI.

Cloudflare Access sits in front of the UI. Only you can open it.
